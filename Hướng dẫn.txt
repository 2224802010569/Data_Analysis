features\learning\workflow\ml_workflow.py có thể chạy độc lập
mỗi lần chạy tạo 5 module trong domain\storage

Khi xóa data.db sau đó chạy test hoặc ml_workflow.py, code sẽ tự tạo data.db
tỉ lệ thành công liên quan đến số lượng dữ liệu lựa chọn, càng nhiều dữ liệu chính xác càng cao (đến 1h trên 90%)